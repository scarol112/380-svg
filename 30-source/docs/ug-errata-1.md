
Address the following errata:

**1)** In section 4.1 the statement structure doesn't show. Angle  brackets in MD files sometimes are considered html. instead of using angle brackets in such cases use bold italic style.

**2)** In section 4 and in 4.1 a reference to section 5 is not an internal link.

**3)** in section 4.4 add a comment that numbers less than one require a leading zero.

**4)** in 4.6 and throughout, refer to the parts of a tuple as members, not slots.

**5)** in Element Assignment change "mutates slot" say "changes member".

**6)** in Element-wise math change "is broadcast across every slot" to "applied to every member of the tuple" and "scalar broadcast" to "same multiplication applied to each member"

**7)** Inline Expressions  "no $ is needed" but is it allowed? Say so.

**8)** in 4.19 step 1 - eid and dim are off by default - aren't they? you won't see the element id or the dimensions

**9)** in 4.19 craft small images to illustrate the steps in the tutorial

**10)** in 5.1 craft small images to illustrate the concepts of radius, sweep, center, and arcstart

**11)** in 5.5 the parameter placeholder names don't show. angle bracets?

**12)** in 5.5 change "Bodies do not" to "Function bodies do not"

**13)** in 5.6 dimensions default to off not on. confirm.

**14)** in 5.7 sxy is mentioned as if all corners are labeled. rectangle corners are currently not, just when dir is chnaged. confirm.

**15)** in 5.8 explain how to get a door to swing in or out and left or right, and what those directions mean for drawing directions 0, 90, 180, 270. create dsl snopppets for all four scenarios with small rendered  images to illustrate.

**16)** in 5.9 the default for eid is off. confirm.

**17)** confirm that left/center/right apply to label

**18)** in 5.15 and 5.16 the parameters for lineto and moveto don't show

**19)** in 5.16 explain the effect of having two target grid piints in "moveto (,) [A=,]"?

**20)** in 5.17, 5.18, and 5.19 parameters don't show

**21)** in 5.18 brx, origx, bry, origy are not defined. Readers will be confused.

**22)** does textappend use left/center/right?

**23)** in 5.23 through 5.27 there is an empty [] set - indicate what that optional item is.






